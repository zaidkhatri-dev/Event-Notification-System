<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Teacher Dashboard</title>
  <style>
    body {
      margin: 0;
      font-family: "Inter", sans-serif;
      background: #f9f9fb;
      color: #333;
    }
    .top-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: #ffffff;
      padding: 1rem 2rem;
      border-bottom: 1px solid #eaeaea;
    }
    .teacher-info {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
    }
    .teacher-info h1 {
      margin: 0;
      font-size: 1.4rem;
      font-weight: 600;
    }
    .teacher-info p {
      margin: 0;
      font-size: 0.9rem;
      color: #666;
    }
    .logout-btn {
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 6px;
      background: #ef4444;
      color: white;
      font-size: 0.9rem;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.2s;
    }
    .logout-btn:hover {
      background: #dc2626;
    }
    .content { padding: 1.5rem; }
    .section h2 { margin-bottom: 1rem; }
    .search-bar {
      width: 100%;
      padding: 0.6rem;
      margin-bottom: 1.5rem;
      border: 1px solid #ddd;
      border-radius: 8px;
      font-size: 1rem;
    }
    .card-container {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 1rem;
    }
    .card {
      background: #ffffff;
      padding: 1rem;
      border-radius: 12px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
      transition: transform 0.2s;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .card:hover { transform: translateY(-4px); }
    .card h3 { margin: 0 0 0.5rem; }
    .card p { margin: 0.2rem 0; font-size: 0.95rem; }
    .no-data { text-align: center; color: #777; margin-top: 2rem; }

    /* Card Actions */
    .card-actions {
      display: flex;
      justify-content: space-between;
      margin-top: 1rem;
    }
    .card-actions form {
      display: inline;
    }
    .card-actions button {
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 0.9rem;
      font-weight: 500;
      transition: background 0.2s;
    }
    .card-actions .update-btn {
      background: #4f46e5;
      color: white;
    }
    .card-actions .update-btn:hover {
      background: #4338ca;
    }
    .card-actions .delete-btn {
      background: #ef4444;
      color: white;
    }
    .card-actions .delete-btn:hover {
      background: #dc2626;
    }

    /* Floating Action Button */
    .fab {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      border: none;
      background: #4f46e5;
      color: #fff;
      font-size: 28px;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      transition: background 0.2s, transform 0.2s;
    }
    .fab:hover {
      background: #4338ca;
      transform: scale(1.05);
    }
  </style>
</head>
<body>
  <!-- Top Bar -->
  <header class="top-bar">
    <div class="teacher-info">
      <h1>Welcome, <%= teacher.userName %> 👋</h1>
      <p>Department: <%= teacher.department %></p>
    </div>
    <form action="/logout" method="POST">
      <button type="submit" class="logout-btn">Logout</button>
    </form>
  </header>

  <!-- Events Section -->
  <main class="content">
    <section id="events-section" class="section active">
      <h2>Events</h2>
      <input type="text" id="eventSearch" class="search-bar" placeholder="Search events..." />

      <div class="card-container">
        <% if (events && events.length > 0) { %>
          <% events.forEach(event => { %>
            <div class="card event-card">
              <div>
                <h3><%= event.title %></h3>
                <p><strong>Description:</strong> <%= event.description || "Not Discloused" %></p>
                <p><strong>Organizer:</strong> <%= event.department %> Department</p>
                <p><strong>Incharge:</strong> <%= event.organizer?.userName || "N/A" %></p>
                <p><strong>Start:</strong> <%= new Date(event.startAt).toLocaleString() %></p>
                <p><strong>End:</strong> <%= new Date(event.endAt).toLocaleString() %></p>
                <p><strong>Mode:</strong> <%= event.mode %></p>
                <% if (event.mode === 'offline') { %>
                  <p><strong>Address:</strong> <%= event.address %></p>
                <% } %>
                <p><strong>Attendees:</strong> <%= event.attendes?.length || 0 %></p>
              </div>
              
              <!-- Card Actions -->
              <% if (event.organizer._id == id) { %>
               <div class="card-actions">
                <form action="/dashboard/editEvent/<%= event._id %>" method="GET">
                  <button type="submit" class="update-btn">Update</button>
                </form>
                <form action="/dashboard/deleteEvent/<%= event._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this event?');">
                  <button type="submit" class="delete-btn">Delete</button>
                </form>
              </div>
              <% } %>
            </div>
          <% }) %>
        <% } else { %>
          <p class="no-data">No events available</p>
        <% } %>
      </div>
    </section>
  </main>

  <!-- Floating Add Button -->
  <button class="fab" onclick="openAddEventForm()">+</button>

  <script>
    // Simple search for events
    function setupSearch(inputId, cardClass) {
      const input = document.getElementById(inputId);
      input.addEventListener("input", function () {
        const filter = input.value.toLowerCase();
        const cards = document.querySelectorAll("." + cardClass);
        cards.forEach(card => {
          const text = card.textContent.toLowerCase();
          card.style.display = text.includes(filter) ? "block" : "none";
        });
      });
    }
    setupSearch("eventSearch", "event-card");

    // Redirect to add event form
    function openAddEventForm() {
      window.location.href = "/dashboard/addEvent";
    }
  </script>
</body>
</html>
